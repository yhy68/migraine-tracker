# 偏头痛记录应用（migraine-tracker）— 项目需求文档

> 整理时间：2026-09-01 ｜ 当前版本：v1.20
> 本文基于本项目全部对话记录（需求提出 → 设计 → 实现 → 测试 → 修复迭代）提炼，描述用户对该项目的**详细需求**与**业务规则**，可作为后续开发的基准规格。

---

## 1. 项目背景与目标

用户需要一款**个人偏头痛发作追踪工具**，用于长期记录每次发作的关键信息，便于回顾发作规律、用药效果与持续时间。核心痛点：发作时记录要快、事后查询要清楚、数据不能丢且需在多设备间一致。

**项目目标**
- 极简记录的偏头痛手账（PWA），离线可用、云端同步。
- 纯前端实现，零后端服务，借助 GitHub 私有仓库作为"云端数据库"。
- 视觉风格简洁一致，支持深浅色主题。

**范围边界（明确不做）**
- 不做复杂医学分析、医生报告、图表诊断（用户明确要求"极简记录为主"）。
- 不做社交/分享功能。

---

## 2. 用户与场景

| 角色 | 描述 |
|------|------|
| 唯一用户（本人） | 偏头痛患者，需随时记录发作；偏好简洁中文交互、结构化操作；移动端与桌面端均使用 |

**典型场景**
1. 发作时：快速打开 → 点"此刻"→ 调疼痛程度 → 保存。
2. 发作结束：补填结束时间（可能跨天）→ 保存。
3. 事后回顾：按时间倒序浏览历史，筛选某时间段，搜索某用药。
4. 多设备：手机记录后，电脑打开能看到同样的记录（云同步）。
5. 换设备/清缓存：重新从云端拉取全部记录。

---

## 3. 功能需求（FR）

### FR-01 记录偏头痛发作（核心）
- 字段：
  - 开始日期、开始时间
  - 结束日期、结束时间（可空，表示持续中/未记录结束）
  - 疼痛程度（1–10 整数，必填）
  - 用药（文本，可空）
  - 备注（文本，可空）
- 校验规则：
  - 开始时间为必填；疼痛程度为必填。
  - 结束时间不得早于开始时间（否则拦截保存）。
  - 备注长度需有合理上限（防滥用/溢出）。

### FR-02 实时云同步
- 增 / 删 / 改任意记录后，**立即**与云端（GitHub 私有仓库 `yhy68/migraine-data` 的 `records.json`）同步。
- 其他设备应能拉取到最新数据并保持一致。
- 同步模型：
  - `syncData()`：拉取 → 合并 → 推送（含 409 冲突重试）。
  - `backgroundSync()`：仅在本地 `dirty` 标记为真时推送。

### FR-03 冲突与删除处理
- 冲突解决：按 `updatedAt` 比较，**本地较新则保留本地**（本地优先策略）。
- 删除传播：删除时记录 `deletedId` 至 `migraine_deleted`；同步成功后清除本地删除标记。
- 网络异常需具备重试/降级能力，不丢失本地数据。

### FR-04 历史记录展示
- 列表按 **开始时间（startTime）倒序**（最新在前）。
- 时间显示必须**精确到分钟（HH:MM）**，不得截断分钟。
- 每条记录卡片展示：日期、时间段、疼痛程度、用药、备注、持续时间（若有结束时间）。

### FR-05 日期选择器
- 外观：输入框 + 日历图标，点击展开日历面板（**非直接铺在大页面上**）。
- 与主题、时间选择器视觉风格**保持一致**（统一圆角、边框、卡片阴影、CSS 变量）。
- 快捷按钮：**仅保留"今天"**，移除"昨天/本周一/本周末/上周/上月"。
- 支持年/月前后导航；禁用日期（受 min/max 约束）不可选。

### FR-06 时间选择器
- 外观：输入框 + 时钟图标，点击展开步进面板（**非直接显示大数字**）。
- 快捷按钮：**仅保留"此刻"**，默认时间为当前时间；移除了"刚刚/15分钟前/…/自定义"等预设按钮。
- 步进交互：
  - **单击一次 = 步进 1 个单位**（小时归小时、分钟归分钟），不得一次跳 2 小时。
  - 支持**长按连发**（约 400ms 后每 80ms 一次）以快速调整。
  - 点击数字可 `prompt` 直接输入精确值。
- 键盘可访问（Enter/空格单步）。

### FR-07 结束日期/时间的选取
- 结束日期**可选任意日期，包括未来日期**（支持跨天发作，如今晚开始、明早结束）；**不得限制为"仅今天"**。
- 结束日期 `min = 开始日期`（结束不得早于开始），无上界（`max` 已移除）。
- 当开始日期变更时，结束日期的 `min` 须同步跟随。

### FR-08 保存后界面行为
- 保存记录后，**不得强制切回"记录表单"页**；应停留在用户当前所在的 tab（记录页或历史页）。

### FR-09 批量操作
- 历史列表支持：进入批量选择模式、单条勾选、全选本页、批量删除。
- 批量删除需二次确认，并同步云端。

### FR-10 筛选 / 搜索 / 分页
- 时间筛选：近 7 天 / 30 天 / 90 天 / 自定义日期区间。
- 关键字搜索：按用药名、备注匹配。
- 长列表分页展示。

### FR-11 导入 / 导出
- 导出：支持 CSV / Excel。
- 导入：支持 JSON 导入，导入时经 `validateRecord()` 校验与清洗（字段规范化、非法值过滤）。

### FR-12 主题与隐私
- 支持浅色 / 深色主题切换（自动 + 手动），切换不得崩溃。
- 应用内提供隐私声明（说明数据存储位置与同步方式）。

---

## 4. 非功能需求（NFR）

| 编号 | 类别 | 需求 |
|------|------|------|
| NFR-01 | 安全 | 历史列表渲染对用药名/备注做 **XSS 转义**（escapeHtml），防止注入 |
| NFR-02 | 离线 | PWA：`manifest.json` + Service Worker 缓存静态资源，LocalStorage 离线优先 |
| NFR-03 | 缓存一致 | 每次发布升级版本号（`index.html` 的 `?v=x.xx` 与 `sw.js` 的 `CACHE_NAME`/ASSETS），确保浏览器拉取新资源；用户需硬刷新 |
| NFR-04 | 可测试 | 具备自动化测试（jsdom 加载真实源码 + 真实 DOM 事件），覆盖数据层/控件/业务逻辑 |
| NFR-05 | 兼容性 | 桌面（鼠标）+ 移动端（触摸/长按）均可正常使用 |
| NFR-06 | 可维护 | 无死代码、逻辑清晰、关键交互有注释；CSS 类与 JS 引用一致 |

---

## 5. 关键业务规则（BR）

- **BR-01** 时间显示格式 `HH:MM`，分钟不可丢失（历史曾因正则 `replace(/:\d{2}$/,':00')` 被清零，已修复）。
- **BR-02** 历史排序严格按 `startTime` 倒序。
- **BR-03** 结束时间 ≥ 开始时间（跨天允许，仅比较 datetime）。
- **BR-04** 时间步进：单击 = 1 单位；长按 = 连发；禁止一次跳 2 单位。
- **BR-05** 结束日期 ≥ 开始日期；结束日期**无 max 上限**（可跨天/未来）。
- **BR-06** 任意增删改须触发 `dirty` 标记并尽快同步云端。
- **BR-07** 日期禁用判定使用**本地日期字符串字典序比较**，规避 `new Date('YYYY-MM-DD')` 被解析为 UTC 午夜导致的时区误禁用。
- **BR-08** 删除采用"标记 + 同步后清除"策略，保证多设备删除最终一致。

---

## 6. 验收标准 / 测试要求

1. **功能验收**：FR-01 ~ FR-12 全部可操作、行为符合上述规则。
2. **自动化测试**：jsdom 测试套件 **47 项用例全部通过**（数据层、TimePicker/DatePicker、App 核心逻辑、UI 一致性）。
3. **真机复测**（建议）：
   - 真实 GitHub API 连通性、多设备同步一致性。
   - Service Worker 实际缓存更新（硬刷新后生效）。
   - 移动端触摸/长按步进体验。
4. **安全验收**：用药/备注含 `<script>` 等恶意内容时页面不执行、仅作文本展示。

---

## 7. 需求落地印证（版本演进）

| 版本 | 对应需求落地 / 修复 |
|------|-------------------|
| v1.16 | FR-06 时间选择器重写为"输入框+面板"，与日期选择器风格统一 |
| v1.17 | NFR-06 清理旧滚轮死代码；FR-12 修复主题切换崩溃；FR-08 修复编辑日期被重置 |
| v1.18 | FR-07 修复结束日期选不了（时区误禁用 + 焦点/点击冲突） |
| v1.19 | FR-06 修复时间步进"按一次跳两小时"（click+mousedown 双触发） |
| v1.20 | FR-05 日期快捷按钮精简为仅"今天"；FR-07 结束日期去掉 max 限制 |

---

## 8. 已知约束与待办

- **Token 安全**：GitHub PAT 仅做 XOR 混淆存储，建议后续改为用户手动输入或更安全方案。
- **冲突策略单一**：当前"本地优先"在多设备同时编辑同一条记录时可能覆盖，可演进为字段级合并。
- **死代码**：`datepicker.js` 的 `handleQuickSelect` 仍保留已移除按钮的分支，可清理。
- **范围外**：会话中曾提及"排班提醒系统（shift-reminder）"的修改，但该仓库在 GitHub 不存在、本工作区无对应代码，**不属于本项目范围**，此处不纳入需求。

---

*本文由项目全部对话记录提炼而成，反映截至 v1.20 的需求基线。*
