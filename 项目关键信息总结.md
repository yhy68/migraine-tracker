# 偏头痛记录应用（migraine-tracker）— 项目关键信息总结

> 整理时间：2026-08-31 ｜ 当前版本：v1.20 ｜ 仓库：yhy68/migraine-tracker（public, main 分支）
> GitHub Pages 部署地址：`https://yhy68.github.io/migraine-tracker/`

---

## 1. 项目概览

| 项 | 内容 |
|----|------|
| 用途 | 个人偏头痛发作追踪：记录发作开始/结束时间、疼痛程度、用药、备注，支持历史查询、筛选、导出与多设备云同步 |
| 形态 | 纯前端 PWA（Progressive Web App），无构建步骤，可直接静态托管 |
| 托管 | GitHub Pages（自动部署 main 分支） |
| 云端数据库 | GitHub 私有仓库 `yhy68/migraine-data`，以 `records.json` 存储记录 |
| 跨设备同步 | 通过 GitHub REST API 读写私有仓库实现 |
| 离线能力 | Service Worker 缓存静态资源，LocalStorage 离线优先 |

---

## 2. 技术栈

- **前端**：原生 HTML / CSS / JavaScript（ES6 class），无框架、无打包器
- **PWA**：manifest.json + Service Worker（sw.js）
- **存储**：浏览器 LocalStorage（主键 `migraine_records`、`migraine_deleted`、`theme` 等）
- **同步**：GitHub API v3（`contents` API 读写 JSON）+ PAT（Personal Access Token）
- **Token 安全**：PAT 经 XOR 简单加密后存于 `js/storage.js` 的 `_tk` 字段（非明文，非高安全级）
- **测试**：jsdom + Node（真实加载源码进 DOM，用真实事件驱动）

---

## 3. 核心文件结构

```
migraine-tracker/
├── index.html          # 入口；引用 4 个脚本 + CSS，均带 ?v= 版本号
├── manifest.json       # PWA 清单（名称/图标/主题色）
├── sw.js               # Service Worker；CACHE_NAME = 'migraine-tracker-v1.xx' 控制缓存失效
├── css/
│   └── style.css       # 全部样式（含 DatePicker / TimePicker 组件样式、响应式、深浅色主题变量）
├── js/
│   ├── storage.js      # 数据层：本地 CRUD、GitHub 同步、合并冲突、导入导出、校验/XSS 清洗
│   ├── datepicker.js   # 日期选择器组件（输入框 + 点击展开日历面板）
│   ├── timepicker.js   # 时间选择器组件（输入框 + 点击展开步进面板）
│   └── app.js          # 核心业务：表单、渲染、筛选/搜索/排序/分页、批量删除、编辑、主题、隐私
└── test/               # 自动化测试（jsdom）
    ├── run-tests.js        # 47 项用例测试套件
    ├── generate-report.js  # HTML 报告生成器
    ├── results.json        # 测试结果（框架产出）
    └── report.html         # 可视化测试报告
```

---

## 4. 核心机制

### 4.1 同步模型
- **`syncData()`（全量拉取合并）**：`pull → merge → push`，处理 409 冲突重试。
- **`backgroundSync()`（轻量推送）**：仅当本地 `dirty` 标记为真时，将本地记录推送到云端。
- **冲突解决**：按 `updatedAt` 比较，本地较新则保留本地版本（"本地优先"策略）。
- **删除传播**：删除时写入 `migraine_deleted` 记录已删 id；同步成功后由 `clearDeletedIds()` 清除本地删除标记（这是正确产品行为）。

### 4.2 版本与缓存失效
- 每次发布：升级 `index.html` 内 4 处 `?v=x.xx`、以及 `sw.js` 的 `CACHE_NAME` 与 `ASSETS` 预缓存列表。
- 部署后浏览器需**硬刷新（Ctrl+Shift+R）** 以让 Service Worker 拉取新资源。

### 4.3 Token 加密
- GitHub PAT 以 XOR 方式加密后存入 `storage.js` 的 `_tk` 字段，运行时解密使用。属轻量混淆，非高安全方案。

---

## 5. 功能清单

| 功能 | 说明 |
|------|------|
| 记录增删改查 | 开始/结束日期时间、疼痛程度（1–10）、用药、备注 |
| 实时云同步 | 增删改立即同步云端，多设备拉取更新 |
| 历史排序 | 按 `startTime` 倒序（最新在前） |
| 筛选 | 近 7 / 30 / 90 天、自定义日期区间 |
| 搜索 | 按用药名 / 备注关键字 |
| 分页 | 长列表分页展示 |
| 批量删除 | 多选 + 全选 + 批量删除 |
| 编辑回填 | 点击记录进入编辑，表单与日期/时间选择器回填 |
| 主题切换 | 浅色 / 深色（自动 + 手动） |
| 数据导出 | CSV / Excel |
| 导入 JSON | `importJSON` 带 `validateRecord()` 校验清洗 |
| 隐私声明 | 应用内嵌隐私说明 |

---

## 6. 版本迭代与缺陷修复史

| 版本 | 关键变更 / 修复 |
|------|----------------|
| v1.16 | TimePicker 重写为「输入框 + 点击展开步进面板」，与 DatePicker 视觉风格统一 |
| v1.17 | ① 删除旧滚轮时间选择器残留死代码 CSS（约 50 行）；② 修复 `applyTheme` 参数遮蔽同名函数导致主题切换崩溃；③ 修复编辑模式发作日期被 `initDatePickers` 重置为今天 |
| v1.18 | 修复「结束日期选不了」：① `DatePicker.isDateDisabled` 改用本地日期字符串比较（规避 `new Date('YYYY-MM-DD')` 被当 UTC 午夜导致的时区误禁用）；② 移除 input 上 `focus→open` 与 `click→toggle` 重复绑定导致的面板闪烁；③ 开始日期变更时同步 `datePickers.end.setMin(val)` |
| v1.19 | 修复时间选择器「按一下跳两小时」：`timepicker.js` 步进按钮同时绑定了 `click` 与 `mousedown` 双触发步进，移除 `click` 重复步进，保留 `mousedown`/`touchstart` 单步 + 长按连发，新增键盘 `keydown` 单步 |
| v1.20 | ① 日期选择器快捷按钮由 6 个精简为仅「今天」；② 结束日期去掉 `max=今天` 限制，允许选未来日期（支持跨天偏头痛） |

### 历史重要 Bug（均已修复并验证）
1. 时间精度丢失：正则 `replace(/:\d{2}$/, ':00')` 把分钟清零 → 改为保留 HH:MM 补秒
2. XSS 漏洞：历史列表药名/备注直接插入 HTML → 增加 `escapeHtml()`
3. 保存后强制切回记录页：异步 `submitRecord` 完成后强行 `renderRecordForm` → 检查 `currentTab`
4. 主题切换崩溃：`applyTheme` 参数 `showToast` 遮蔽同名函数 → 改名 `doToast`
5. 编辑日期被重置：`initDatePickers` 10ms 后重建选择器覆盖 prefill → 尊重已有 prefill
6. 结束日期无法选择：时区误禁用 + 焦点/点击冲突（见 v1.18）
7. 时间步进跳两小时：click+mousedown 双触发（见 v1.19）
8. 结束日期限制为今天 + 快捷按钮过多（见 v1.20）

---

## 7. 测试

- **测试套件**：`test/run-tests.js`，使用 jsdom 将 `storage.js / datepicker.js / timepicker.js / app.js` 原样加载进真实 DOM，用真实 DOM 事件驱动，mock GitHub API 与浏览器 API。
- **覆盖**：数据层（增删改/合并/校验/导入导出/同步/409 重试）、TimePicker / DatePicker 控件、App 核心业务（提交校验/XSS/排序/筛选/搜索/分页/批量删除/编辑回填/主题切换）、UI 样式一致性。
- **结果**：**47 项用例全部通过（47/47）**。
- **报告**：`test/report.html`（HTML 可视化报告，由 `generate-report.js` 生成）。

---

## 8. 部署与运维

1. 代码推送到 `yhy68/migraine-tracker` 的 `main` 分支 → GitHub Pages 自动更新。
2. 每次发布务必升级版本号（`?v=` 与 `CACHE_NAME`）以确保缓存失效。
3. 终端用户需**硬刷新（Ctrl+Shift+R）** 才能加载新 Service Worker 与脚本。
4. `.gitignore` 已排除 `test/node_modules/` 与 `test/package-lock.json`（仅保留 `test/package.json` 以便复现依赖）。

---

## 9. 已知限制 / 待办建议

- **Token 安全**：PAT 仅 XOR 混淆，建议后续改为用户手动输入或更高安全方案（如 OAuth）。
- **冲突策略单一**：当前为「本地优先」，多设备同时编辑同一条记录可能覆盖，可考虑更细的字段级合并。
- **死代码**：`datepicker.js` 的 `handleQuickSelect` 仍保留 `yesterday/week-start/...` 分支（虽按钮已移除），可后续清理。
- **未覆盖测试**：真实 GitHub API 连通性、Service Worker 实际缓存行为需在真实浏览器/外网环境复测。

---

*本文件由自动化整理生成，反映截至 v1.20 的项目状态。*
